# AGENTS 第七版独立复评

> 状态：未通过，已进入第八版
> 需求卡：`../requirements_2026-08-27_agents-iterative-validation.md`

## 议题

1. 足够精简，且每类决定只有一个权威 owner。
2. 人类友好，每条规则只有一个主要判断，首次读者可快速找到入口。
3. 无语义损失，包括主体、强度、触发条件和例外。
4. 自动静态验证可信，不依赖自证循环或只看收据字段。

## 本轮候选

- 工厂根 125 行、58 个项目符号；Template 120 行、43 个项目符号。
- 补回 Spike、主观体验、每项目 `.venv`、自改审计、gap/degraded、check/dry-run 零写语义。
- 完整候选 baseline 与同步器由唯一锚点生成、校验 hash，并从临时候选路径加载执行。
- parser 只允许 README append；当时声称“完整项目树”证明 plan/阻断/no-op 零写和失败回滚。
- 工厂根项目区、Template、三个嵌套指令当时声称受 worktree/staged 全文 hash 门约束。
- 语义来源文件锁 hash，每条红线恰好映射一次，高风险项检查同句关系；活跃候选面执行旧指针扫描。

## 自动收据

```text
proposal-contract: PASS
```

## 第一轮

### 评审 A

- 精简和人类友好通过；无语义损失、验证可信不通过。
- 确认丢失或弱化：性能测量方法、换机配置与绝对路径的禁止强度、focus 的范围变化与原因、Spike 触发范围、模块职责/依赖方向、失败后再次修改前取证。
- 语义合同把同文件其他位置的关键词误当作目标规则，存在确定性假阳性。

### 评审 B

- 同样只通过精简和人类友好。
- 另确认丢失：第 4 次停止时列出已试方案和未验证假说、骨架戳不得代替业务版本、`$summary` 一次只更新一个当前主 Memory。
- 验证器排除 `.git/.venv/__pycache__` 却声称完整项目树；同步器未走子进程 CLI；五个工厂受保护面的负测矩阵不完整；最终 Skill 关系仍只在 patch JSON 上匹配。

## 第二轮收敛

- 双方一致：第七版不能放行。
- 第八版必须定点恢复上述语义，把高风险规则改为主体、强度、触发、顺序同关系匹配，并覆盖 Template 红线。
- 必须新增真实 CLI blocker、五面 worktree/staged 破坏矩阵、最终 Skill 文本检查；迁移账本不得继续使用易漂移的候选行号。
- 无法覆盖 `.git/.venv/__pycache__`、全部 `--check/--dry-run` 命令和全部历史文本时，必须准确缩小验证口径，不得继续使用“完整 / 全部”表述。

## 四项结论

- 精简：通过。
- 人类友好：通过。
- 无语义损失：不通过。
- 自动静态验证可信：不通过。
