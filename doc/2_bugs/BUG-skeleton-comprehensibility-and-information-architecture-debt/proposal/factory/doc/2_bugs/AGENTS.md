# 工厂 Bug 文档指令

本文件只约束 `doc/2_bugs/**`；从项目根启动的任务必须先按根 `AGENTS.md` 的目录索引读取本文件。

## 关闭证据

- 工厂 Bug 关闭时必须分别记录源码、产品传播、工厂自验证镜像（dogfood）、下游样例回归（fixture）、真实下游和运行时（runtime）六类证据。
- 未执行或拿不到的证据必须明确标为未验证；禁止用自动测试替代真实下游或 runtime 收据。
- 缺少适用证据时禁止把 Bug 标为已解决；废弃或不再处理必须记录决定和影响。
