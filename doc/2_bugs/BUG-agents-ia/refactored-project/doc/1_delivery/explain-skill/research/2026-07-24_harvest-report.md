# `explain` Skill 回灌报告

**状态：** pending  
**来源：** `D:\Quant\StratusAgent` 项目级 skill  
**日期：** 2026-07-24

## 结论

建议将已在下游使用的 `explain` 回灌为 BridgeForge 的独立用户级 skill，目标为 `skills/explain/SKILL.md`。它不是项目模板，也不应改写任何项目级 `AGENTS.md` / `CLAUDE.md` 规则。

## 已核验的候选能力

下游 skill 的行为完整且范围明确：

1. 触发：用户显式调用 `$explain`，或要求“白话解释”“讲人话”“举例”、表示未理解时触发。
2. 输出顺序：一句话结论 → 白话解释 → 贴近当前话题的例子。
3. 正确性边界：保留条件、不确定性和限制；不能把“可能”说成“必然”；缺少原文或上下文时明确说明，不能猜测。
4. 范围边界：只负责解释，不重新设计需求、不实施代码、不扩张任务。

该能力已脱离量化交易项目语境：源文件不含项目名、内部包名、凭证、内部 URL、提交哈希、绝对路径或业务专属术语。示例仅要求使用“当前对话、项目或用户提出的场景”，可直接作为通用占位表述。

## 上游落点判断

`doc/0_architecture/design/reverse-sync-playbook.md` 允许将“下游已实测验证的单条 `SKILL.md` 修改”回灌至 `skills/<name>/SKILL.md`。本候选满足这一例外：它是独立、单文件、非业务专属的用户级 skill。

不推荐落入 `templates/`：模板用于生成项目骨架，而 `explain` 是按需发现、独立维护的通用交互能力。

## 建议的后续处理

1. 将经过复核的内容写入 `skills/explain/SKILL.md`。
2. 按上游现行发布规则递增版本，并在 `CHANGELOG` 添加 `[product]` 条目。
3. 在 reverse-sync playbook 的反哺日志中记录日期、来源项目、目标文件和摘要。
4. 运行现有 skill 元数据与分发一致性检查，再审阅实际 diff 和脱敏扫描结果。

本报告只提出候选和落点；尚未新增上游 skill、修改版本、更新日志或提交推送。
