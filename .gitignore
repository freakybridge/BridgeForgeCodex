# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# 已退役 Python 工具留下的本机环境与缓存（不属于产品资产）
.venv/
__pycache__/
*.py[cod]

# Claude Code 本地配置
.claude/settings.local.json

# Hook 运行时临时产物（session_snapshot 快照 / focus 锚 / 各 hook 日志 / spinoff 调查
# 草稿）——全是本机临时产物，含个人绝对路径（C:\Users\bridg）与 transcript 文件名，
# 绝不可进公开库。整目录忽略，避免 spinoff/ 等新子目录漏网误提交。
.runtime/
